# docker-volume
Docker volumes persist data generated by a container outside of the container's own filesystem, so the data survives container removal. This note walks through the commands run in the terminal to spin up a MongoDB container and create/inspect a named volume for it.
